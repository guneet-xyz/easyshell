name: test problem (combine-json)

on:
  pull_request:
    paths:
      - ".github/workflows/autogenerated-test-combine-json.yml"
      - "packages/problems/data/problems/combine-json/**"
      - "packages/env/**"
      - "packages/utils/**"
      - "apps/submission-manager/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
        name: Install dependencies
      - run: pnpm run build combine-json
        working-directory: ./packages/problems
      - run: pnpm run test combine-json
        working-directory: ./packages/problems
