name: test problem (csv-to-json)

on:
  pull_request:
    paths:
      - ".github/workflows/autogenerated-test-csv-to-json.yml"
      - "packages/problems/data/problems/csv-to-json/**"
      - "packages/problems/env.ts"
      - "packages/utils/**"
      - "apps/submission-manager/utils.ts"
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        name: checkout

      - uses: pnpm/action-setup@v5
        name: setup pnpm

      - uses: actions/setup-node@v4
        name: setup node
        with:
          node-version: 22.14.0
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile
        name: install dependencies

      - run: pnpm run build csv-to-json
        name: build problem
        working-directory: ./packages/problems

      - run: pnpm run test csv-to-json
        name: test problem
        working-directory: ./packages/problems
