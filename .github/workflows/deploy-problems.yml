name: Deploy Problems
on:
  workflow_dispatch:
    inputs:
      problem:
        description: "Problem to deploy"
        required: true
        default: "all"
        type: string
jobs:
  push:
    runs-on: ubuntu-latest
    environment: registry-push
    steps:
      - name: tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_CLIENT_SECRET }}
          tags: tag:ci
      - name: docker login
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0
      - run: corepack enable
        name: Enable corepack
      - run: pnpm install --frozen-lockfile
        name: Install dependencies
      - run: pnpm tsx scripts/problems/build.ts ${{ inputs.problem }}
        name: build
      - run: pnpm tsx scripts/problems/test.ts ${{ inputs.problem }}
        name: test
      - run: pnpm tsx scripts/problems/push.ts ${{ inputs.problem }}
        name: push
        env:
          DOCKER_REGISTRY: ${{ vars.DOCKER_REGISTRY }}
