// E2E global setup — boots the full Coordinator+Runner stack.
// Filled in by task T6 (E2E test scenarios).
export async function setup() {
  // TODO(T6): boot Postgres Testcontainer + coordinator + runner processes
}

export async function teardown() {
  // TODO(T6): stop all processes + testcontainer
}
